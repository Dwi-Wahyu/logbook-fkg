module.exports = {
  apps: [
    {
      name: "logbook-fkg",
      script: "node_modules/next/dist/bin/next",
      args: "start",
      interpreter: "bun", // Menggunakan Bun runtime untuk menghemat penggunaan memori
      autorestart: true,
      watch: false,
      max_memory_restart: "500M",
      env: {
        NODE_ENV: "production",
        PORT: 3000,
        AUTH_TRUST_HOST: "true",
      },
    },
  ],
};
